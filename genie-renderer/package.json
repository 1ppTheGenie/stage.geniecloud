{
	"name": "genie-renderer",
	"version": "2.0.1",
	"description": "Processing script for the creation of image/PDF assets from GenieHub. v2.0.0",
	"main": "index.js",
	"scripts": {
		"preaws": "node build.mjs && tar -a -c -f genie-renderer.zip ./build/index.js",
		"preaws-us": "node build.mjs && tar -a -c -f genie-renderer.zip ./build/index.js",
		"create": "aws lambda create-function --function-name genie-renderer --runtime nodejs18.x --zip-file fileb://genie-renderer.zip --handler ./build/index.renderer ",
		"add-layers": "aws lambda update-function-configuration --function-name genie-renderer ",
		"aws": "aws lambda update-function-code --function-name genie-direct --zip-file fileb://genie-renderer.zip",
		"aws-us": "aws lambda update-function-code --function-name GenieRenderer --zip-file fileb://genie-renderer.zip --profile genie-hub-active --region us-west-1",
		"create-us": "aws lambda create-function --function-name GenieRenderer --runtime nodejs18.x --zip-file fileb://genie-renderer.zip --handler ./build/index.js --profile genie-hub-active --role arn:aws:iam::199352526440:role/genie_renderer --region us-west-1"
	},
	"keywords": [],
	"author": "Dynamic Array Ltd",
	"license": "ISC",
	"dependencies": {
		"@aws-sdk/client-s3": "^3.332.0",
		"@aws-sdk/client-sqs": "^3.332.0",
		"pdf-lib": "^1.17.1",
		"puppeteer-core": "22.5.0",
		"sharp": "^0.32.1",
		"@sparticuz/chromium": "122.0.0"
	},
	"devDependencies": {
		"esbuild": "^0.17.19",
		"global": "^4.4.0"
	}
}
